<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nala.sharding.mapper.UserMapper">

    <insert id="save">
        insert into user values(#{user.id},#{user.name},#{user.type},#{user.createTime},#{user.updateTime});
    </insert>
</mapper>
